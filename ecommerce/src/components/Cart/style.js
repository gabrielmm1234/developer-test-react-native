import { StyleSheet } from 'react-native';

export const styles = {
    cartContainer: {
        flexDirection: 'row',
    },
    badgeColor: {
        color: 'white',
    },
    badgeBackground: {
        backgroundColor: 'red',
    }
}