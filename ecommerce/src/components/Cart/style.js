import { StyleSheet } from 'react-native';

export const styles = {
    cartContainer: {
        flexDirection: 'row',
        marginRight: 10,
    },
    badgeColor: {
        color: 'white',
    },
    badgeBackground: {
        backgroundColor: 'red',
    },
    icon: {
        color: 'black'
    }
}